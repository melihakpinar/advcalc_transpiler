#include <stdio.h>
#include "operators.h"

int32_t main() {
    while(1){
        printf(">");
        char input[300];
        fgets(input, 300, stdin);
        // TODO: evaluate input
    }
    return 0;
}
